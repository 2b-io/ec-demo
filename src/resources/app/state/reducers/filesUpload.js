const files = (state = [], action) => {
  switch (action.type) {
    case 'ADD_FIle':
      return state
    case 'DELETE_FILE':
      return state
    default:
      return state
  }
}

export default files
